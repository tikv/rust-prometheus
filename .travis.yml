language: rust

os:
 - linux
 - osx

rust:
  - stable
  - beta

env:
  - RUSTFLAGS=-Dwarnings

script:
  - cargo test
  - cargo test --features="process"

matrix:
  include:
  - rust: nightly
    script:
      - cargo test --features="dev nightly"
